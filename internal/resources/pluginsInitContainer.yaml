volumes:
  plugins:
    mode: volume
    volume:
      emptyDir: {}

initContainers:
  - name: init-plugins
    image: neo4j:4.3.0
    command: ['bash', '-c', "cp /var/lib/neo4j/labs/* /plugins"]
    volumeMounts:
      - name: plugins
        mountPath: /plugins
